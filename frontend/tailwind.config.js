export default {
    content: [
        "./index.html",
        "./src/**/*.{js,jsx}",
    ],
    theme: {
        extend: {
            colors: {
                primary: '#10b981',  // Green (agriculture/growth)
                secondary: '#f59e0b' // Orange (warmth/trust)
            }
        },
    },
    plugins: [],
}
